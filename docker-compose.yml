services:
  we-track:
    image: ghcr.io/souravshrestha/we-track:latest
    ports:
      - "3000:3000"
    volumes:
      - we-track-data:/data
      - /Users:/Users:ro
    environment:
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  we-track-data:
